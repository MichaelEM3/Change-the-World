<%= form_for(@club) do |f| %>
  <% if @club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@club.errors.count, "error") %> prohibited this club from being saved:</h2>

      <ul>
      <% @club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<%end%>



<%= form_for @club, html: {multipart: true} do |f| %>
    <p>
      <%= f.label :title%>
      <%= f.text_field :title %>
    </p>
    <p>
      <%= f.label :description %>
      <%= f.text_area :description %>
    </p>
    <p>
      <%= f.label :location %>
      <%= f.text_field :location %>
    </p>
    <p>
      <%= f.label :image, "Attach an Image" %><br>
      <%= f.file_field :image, placeholder: "Click to attach a club image" %>
    </p>
    <p>
      <%= f.label :thumbtag %>
      <%= f.text_field :thumbtag, placeholder: "Space tags by commas"%>
    </p>
  <!--   <p>
     f.label :tag_list <br />
     f.text_field :tag_list 
    </p> -->
    <p>
      <%= f.submit %>
    </p>
<% end %>
